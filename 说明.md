# 本地划词听译助手

> 在 Chrome 浏览器中选中网页文本，一键本地朗读，或使用本地中介服务实际发现的 Ollama 模型翻译。
> 翻译设置先选择“本地 Ollama”或“项目服务器”，再只显示该来源当前真实可达、可用于文本生成的模型。
> `Read` 会先清洗文本：英文公式场景会先读正文、后台处理公式，播放到公式时如果还没处理好再等待；中文、代码块、URL、表格碎片等不会直接送进英文 TTS。

---

## 📐 系统架构

```text
┌──────────────────────┐       仅访问本机回环       ┌───────────────────────┐
│ Chrome + Tampermonkey│  ──────────────────────►  │ 本地 FastAPI 中介   │
│ Read / Translate     │      127.0.0.1:5000      │ /tts → 按需 Kokoro  │
│ 后台状态投影         │  ◄──────────────────────  │ /translate → Ollama │
└──────────────────────┘                            └──────────┬────────────┘
           │ localreadtranslate://              ├─► 本机 Ollama
           │ start / ollama / remote             │
           ▼                                     └─► 已配置的远程 Ollama
┌──────────────────────┐                     SSH/API
│ Windows 托盘程序     │
│ 启动或唤醒 FastAPI   │
└──────────────────────┘
```

油猴脚本不会拿到 SSH 凭据，也不会直接访问 Ollama。浏览器只与本地 API 交互；托盘程序负责进程启动、SSH/API 配置和隧道生命周期。Word/WPS 文档加载项通过另一个严格回环宿主 `127.0.0.1:5443` 读取白名单资源并同源代理 `/api/*`：翻译、朗读稿准备和模型状态沿用网页端的来源优先契约，选择远程模型时只把当前文档选区经本地 FastAPI 发往托盘已配置的服务器；LaTeX 是唯一外部公式交换格式，DOCX/OMML 只用于文档内部的原生公式插入与读取。

---

## 为什么选 Kokoro TTS？

| 特性 | 说明 |
|------|------|
| 🏆 发音质量 | 开源本地 TTS 中最自然标准的英文发音，被称为"TTS 界的 LLaMA 时刻" |
| ⚡ 极速推理 | 82M 超轻量模型，RTX 4070 Super 上几乎实时生成 |
| 🌊 流式播放 | Chrome 使用 `/tts/stream` + MediaSource + WebM/Opus，长文本可边生成边播放 |
| 📦 压缩输出 | `/tts` 支持 OGG/Opus，默认 WAV 保持兼容 |
| 🛡️ 智能调度 | 后端支持智能排队机制与客户端断开感知，防止并发导致显卡 OOM 及僵尸进程 |
| 🧹 优雅清理 | 前端内置 MutationObserver 与 AbortController，页面路由跳转或切换时自动清理内存与请求 |
| 📊 进度反馈 | 浮动按钮显示横向填充进度；流式阶段先显示已播放秒数 |
| 🧠 渐进朗读稿 | 英文含公式时先读正文，后台用本地模型处理公式；到公式处若尚未完成再等待，避免整段被公式拖住 |
| ∑ 公式友好 | MathJax/MathML/LaTeX 会优先提取语义公式；朗读时转成流畅口语，翻译时在前端渲染为带上下标的易读公式 |
| ⎘ 选中复制 | `Copy` 不调用翻译，保留正文段落；行内/独立公式会自动扩展并统一复制为 `$...$` / `$$...$$` |
| 📝 Word/WPS 可安装文档插件 | 同一任务窗格按网页端布局提供选区翻译、本地朗读和双向公式/LaTeX 转换 |
| 🛡️ Trusted Types 兼容 | 前端 UI 不再使用 `innerHTML` 等 HTML 字符串注入，适配 Gemini 等严格页面 |
| 🧭 上下文参考 | 翻译请求可附带附近正文作为参考，只用于术语和指代消歧；真正翻译和输出的只有选中内容；选择远程模型时该上下文也会发送到对应服务器 |
| 🧮 模型上下文预算 | 4B 模型翻译和公式朗读不参考上下文，9B/14B/更大模型按大小逐级增加上下文长度 |
| ⚡ Qwen3 no-think | `qwen3:14b`、QwQ、DeepSeek-R1 等推理模型会通过 Ollama `think: false` 关闭思考过程，减少翻译和朗读准备延迟 |
| 🧭 后台驱动设置 | 在线时始终提供“本地 Ollama / 项目服务器”两个来源入口；模型列表只显示当前选中来源的真实生成模型，不显示静态、自定义或 embedding/reranking 假选项 |
| 📌 模型常驻/卸载 | Advanced 中只显示当前可执行的常驻/卸载操作；卸载后仍在运行会如实显示 `still_running` |
| ∑ 4B 保守公式读法 | 选择 4B 模型时，常见公式优先用本地字面规则朗读，例如 `D_I` 读作 `D sub I`，`\hat{B}(x)` 读作 `B hat of x` |
| ∑ 数学术语表 | `config/math_glossary.json` 维护 50+ 个核心数学符号的直接读法和上下文读法，例如右箭头可按语境读作映射到、趋向于、推导出、得到或右箭头 |
| 🧭 避让选区 | Read 和 Translate 可同时进行；按钮行固定在选区下方，译文卡片会根据空间避让，减少遮挡正文 |
| ∑ 局部公式恢复 | 只选中 MathJax/MathML/KaTeX 公式的一部分时，会尽量扩展到完整公式框；选中整句时保留公式前后文字 |
| 🎭 17 种声音 | 内置美式男声、女声、英式声音，无需自备参考音频 |
| 🔒 隐私边界 | 浏览器只访问本机中介；选择远程模型时，文本才由中介发送到托盘已配置的服务器 |
| 💰 免费无限 | 开源免费，无 API 调用费用 |

---

## 💻 系统要求

| 项目 | 要求 | 当前状态 |
|------|------|---------|
| 操作系统 | Windows 10/11 | ✅ |
| 显卡 | NVIDIA GPU（可选，CPU 也可运行） | 推荐 CUDA 显卡 |
| Python | 3.10 | 通过 Conda 管理 |
| eSpeak-NG | 语音音素引擎（必需） | 需安装 |
| 浏览器 | Chrome + Tampermonkey 扩展 | 需安装 |
| Pandoc 3.x | Word/WPS 原生公式转换时必需 | 普通浏览器听译不需要 |

---

## 🚀 快速开始（5 步走）

### 第 1 步：安装 eSpeak-NG

Kokoro 依赖 eSpeak-NG 进行音素转换，**必须先安装**：

1. 访问 [eSpeak-NG Releases](https://github.com/espeak-ng/espeak-ng/releases)
2. 下载最新的 Windows `.msi` 安装包并安装
3. **关键**：将 eSpeak-NG 安装路径（通常是 `C:\Program Files\eSpeak NG`）添加到系统环境变量 `Path` 中
4. 重启终端使环境变量生效

验证安装：
```powershell
espeak-ng --version
```

### 第 2 步：创建 Conda 环境 & 安装依赖

```powershell
# 创建独立的 Python 3.10 环境
conda create -n kokoro-tts python=3.10 -y
conda activate kokoro-tts

# 安装 PyTorch（CUDA 12.4，兼容你的 CUDA 13.1 驱动）
pip install torch torchaudio --index-url https://download.pytorch.org/whl/cu124

# 安装项目依赖
pip install -r requirements.txt
```

或者直接双击 **`setup.bat`** 一键完成。

### 第 3 步：启动 TTS 服务器

```powershell
conda activate kokoro-tts
python server.py
```

服务启动时只初始化 API 和 bundled FFmpeg。Kokoro 模型在第一次点击 `Read` 时才按需下载/加载，因此仅远程翻译不会占用本机 GPU。访问 `/health` 时，`api_ready: true` 表示 API 可用，`tts_model_loaded` 单独表示 Kokoro 是否已加载。

也可以直接双击 **`start.bat`** 一键启动。

推荐双击 **`Kokoro TTS.bat`** 启动托盘程序；它不依赖 Windows 的 `.pyw` 文件关联，并会为当前用户创建或修复 `localreadtranslate://` 协议。网页只使用固定的 `start`、`ollama`、`remote` 三个无参数操作。
`start.bat` 和 `Kokoro TTS.bat` 会直接定位 `kokoro-tts` 环境里的 Python，日常启动不需要先执行 `conda init`。`start.bat` 只启动 FastAPI，不会建立 SSH 隧道；使用远程服务或网页一键启动时请用托盘程序。`Kokoro TTS.pyw` 保留为无黑窗口启动器，但只有在 Windows 已把 `.pyw` 关联到 Python 时才适合双击。

也可手动注册或修复协议：

```powershell
conda run -n kokoro-tts python windows_protocol.py register
```

注册位于当前用户的 `HKCU` 注册表，无需管理员权限。其中保存项目的绝对路径，如果移动或重命名项目文件夹，需重新执行注册。

如需只移除这个当前用户协议处理程序：

```powershell
conda run -n kokoro-tts python windows_protocol.py unregister
```

### 第 4 步：安装油猴脚本

1. 在 Chrome 中安装 [Tampermonkey 扩展](https://chrome.google.com/webstore/detail/tampermonkey/dhdgffkkebhmkfjojejmpbldmpobfkfo)
2. 推荐从 Greasy Fork 安装正式发布版
3. 如需开发版，可打开 GitHub raw 脚本：`https://raw.githubusercontent.com/Yan-ShiBo/LocalReadTranslate/main/tts-userscript.js`
4. Tampermonkey 弹出安装页后确认安装

### 第 5 步：开始使用！

1. 确保 TTS 服务器正在运行（第 3 步）
2. 打开任意英文网页
3. **选中一段网页文本** → 自动弹出 `Read`、`Translate` 和 `Copy` 按钮
4. 点击 `Read` 本地朗读英文内容并后台处理公式，点击 `Translate` 使用当前选中的本机或远程 Ollama 模型，或点击 `Copy` 保留正文段落并把行内/独立公式统一复制为 `$...$` / `$$...$$` 🎉
5. 齿轮面板先选来源、再选该来源的真实模型：
   - 中介服务在线时始终显示 **Local Ollama** 和 **Project Server** 两个来源入口，即使其中一个暂时离线；
   - 选择 **Local Ollama** 后只显示本机模型；本机 Ollama 离线时显示 **Start**，点击后通过 `localreadtranslate://ollama` 请求托盘启动已安装的 `ollama serve`，并等待发现完成；
   - 选择 **Project Server** 后只显示服务器模型；未连接时显示 **Connect** 并通过 `localreadtranslate://remote` 打开托盘的 `Remote Service` 窗口，已连接时不再重复要求连接；
   - 已连接的当前来源没有生成模型时，仍明确显示已连接，只提示安装生成模型，不伪造默认选项；
   - 服务离线时只显示 **Start local service**，模型、翻译测试、Advanced 和 Read aloud 都隐藏；按钮打开 `localreadtranslate://start` 并轮询 `/health` 约 20 秒；
   - 翻译健康请求失败时显示明确的 **Unavailable**，不会让徽标停在 **Checking**、正文却显示失败；
   - 测试翻译、来源/模型切换和常驻/卸载后的后台刷新保留最近一次有效来源视图，不再反复闪回 **Checking translation sources...**；切换来源、模型或目标语言会清除不再匹配的旧测试结果；
   - 目标语言和当前模型适用的常驻/卸载操作位于 **Advanced**，声音与语速位于折叠的 **Read aloud**；
   - SSH/API 配置和连接仍只在托盘 `Remote Service` 中完成，网页不保存远程凭据。

> 如果在 Gemini 等页面右下角齿轮都没有出现，优先检查 Tampermonkey 是否允许在 `gemini.google.com` 运行，以及 Chrome 扩展的“站点访问权限”是否允许该域名；如果齿轮出现但选区按钮不出现，脚本会通过 `selectionchange` 兜底监听动态页面选区。脚本 UI 已移除 `innerHTML` 等 Trusted Types 会拦截的写入点。

> ⌨️ **快捷键**：选中文本后按 **Ctrl+Shift+S** 也可直接朗读。
> 后端省略模型参数时仍可用 `translategemma:4b` 作为服务端回退；网页不会把它伪装成已安装模型。可通过 `OLLAMA_TRANSLATE_MODEL`、`OLLAMA_READ_MODEL`、`OLLAMA_FORMULA_MODEL` 覆盖后端回退。
> **Advanced** 中的 **Load & keep / Keep loaded** 会用 Ollama `keep_alive: -1m` 常驻当前模型；模型运行时显示 **Unload**，模型已退出但中介仍保留 pin 时显示 **Remove keep-alive**，清 pin 不会重新加载模型。
> 上下文长度会按模型大小自动裁剪：4B 翻译和公式朗读不参考上下文，14B 会保留更多上下文，避免小模型被长上下文干扰。
> 使用 `qwen3:14b`、QwQ、DeepSeek-R1 等推理模型时，服务端会自动向 Ollama 传入 `think: false`，让翻译和朗读准备直接输出结果。
> 4B 公式朗读会尽量不用上下文判断语义，而是采用最常见、最保守的字面读法；规则处理不了的复杂公式才交给 Ollama。
> 公式口语化会参考 `config/math_glossary.json`，先用规则处理常见符号，再把同一术语表交给 Ollama 处理复杂公式；当前表吸收了 v2 参考表，覆盖箭头、上下标、集合、逻辑、求和、积分、偏导等核心符号。
> 如果朗读稿准备接口不可用，`Read` 会退回到 `/translate` 并指定翻译成 English 后再朗读。

---

## 可安装的 Word / WPS 文档加载项

先保存文档并关闭 Word、WPS，然后双击：

```powershell
.\install-document-addins.bat
```

重新打开应用后：

- Word：**开始 → 加载项 → LocalReadTranslate 文档工作台**；
- WPS Writer：**LocalReadTranslate → 阅读与公式**。

任务窗格与网页端脚本采用同一套信息层级：

- 首屏只展开 **翻译**：先选择“本地 Ollama”或“项目服务器”，模型下拉框只列出当前可达来源真实发现的文本生成模型；翻译结果可复制，也可直接替换当前文档选区；
- **高级**：只放目标语言和当前确实可执行的模型常驻/卸载动作；
- **朗读**：使用 `/voices` 的真实声音/语速列表；纯英文无需 Ollama，中文或含公式选区会先用当前选中模型调用 `/read/prepare`，再由本机 Kokoro 生成并播放 WAV；
- **公式与 LaTeX**：混合正文与 `$...$` / `$$...$$` 通过本地 Pandoc 转成 Word/WPS 内部可编辑公式；反向复制时，Word 发送 Flat OPC，WPS 使用原生 `Range.Copy/Paste` 生成当前用户临时目录下的随机一次性 DOCX。服务严格校验路径、文件名、大小、ZIP 条目和结构，控制器无论成功或失败都会清理临时文件，剪贴板最终只有规范纯文本 LaTeX。

任务窗格默认选择本地来源，并分别保存来源、每个来源上次模型、目标语言、声音和语速。初始化或主动点击“重试”时，公式、翻译和声音能力并行发现一次；普通翻译、朗读和公式按钮直接复用缓存状态，不会每次重新显示 “Checking translation sources...”。只有用户明确点击 **启动/连接** 后才轮询翻译来源，并在就绪后自动刷新真实模型列表。公式转换不依赖 Ollama，不会启动本机 Ollama，也不会改变远程隧道。

安装器只写当前用户配置：注册精确的 Office manifest，备份并合并一个 WPS `publish.xml` 项，同时在需要时启动严格绑定 `127.0.0.1:5443` 的加载项宿主。它不会静默创建或信任根证书。默认 HTTP 仅用于本机桌面开发；如果调用者已经拥有受信任证书，`addon_host.py` 可显式使用 `--cert` / `--key`。

卸载只删除本项目的两个注册和安装器拥有的独立宿主：

```powershell
.\uninstall-document-addins.bat
```

50 个常见公式的测试语料生成了 50 个 OMML 公式，反向转换仍识别为 50 个公式；Microsoft Word 16.0 与 WPS Writer 12.1.0.26895 均确认包含 50 个原生公式、61 个段落。

Word 已跑完真实按钮双向链路：`测试公式 $x^2 + y^2 = z^2$ 和 $\frac{a}{b}$。` 先成为两个可编辑原生公式，再复制为 `测试公式 $x^{2} + y^{2} = z^{2}$ 和 $\frac{a}{b}$。`。WPS Writer 12.1.0.26895 也已完成真实双向验收：`WPS 测试：$x^2 + y^2 = z^2$，以及 $\frac{a}{b}$。` 转成两个可编辑原生公式，反向复制显示“已复制 2 个公式”，Windows 剪贴板只有一份 `WPS 测试：$x^{2} + y^{2} = z^{2}$，以及 $\frac{a}{b}$。`，没有重复段落。

新增文档助手链路已通过加载项实际使用的 `127.0.0.1:5443/api/*` 代理验证，测试只选择 `remote:project-server:qwen3:30b`：翻译返回简体中文并保留 `$x^2 + y^2 = z^2$`；朗读稿为英文公式口语；本机 TTS 返回 `200 audio/wav`、307,244 字节且 RIFF 头正确。测试后 30B 模型已显式卸载，项目服务器仍连接，本机 Ollama 仍关闭。390 px 与 280 px 隔离布局检查均无横向溢出。该证据验证 API/代理与布局，不冒充新翻译/朗读按钮已在真实 Word/WPS 文档中点击；宿主按钮级实机证据目前仍是上一段的公式双向链路。

完整安装、故障排查和证据见 [`addons/README.md`](addons/README.md) 与 [`docs/iteration-8-2026-07-23-office-wps-document-assistant.md`](docs/iteration-8-2026-07-23-office-wps-document-assistant.md)。

---

## 🎭 可用声音列表

声音、语速和默认值统一维护在 `config/tts_catalog.json`。API、托盘菜单、
油猴脚本和内置测试页均从这一个 catalog 生成，避免各处列表不一致。

---

## 📁 项目文件说明

| 文件 | 说明 |
|------|------|
| `说明.md` | 本文档 |
| `server.py` | FastAPI 本地 TTS 服务器，加载 Kokoro 模型并暴露 `/tts` API |
| `document_formula.py` | 规范 LaTeX 解析、DOCX/OMML 安全校验与 Pandoc 双向转换 |
| `addons/` | Word/WPS manifest、ribbon、共享文档助手任务窗格、公式控制器与宿主适配器 |
| `addon_host.py` | 严格绑定 `127.0.0.1:5443` 的加载项资源宿主与同源 API 代理 |
| `addin_registration.py` | WPS `publish.xml` 幂等合并与精确删除 |
| `audio_encoding.py` | bundled FFmpeg 封装，用于 OGG/Opus 与 WebM/Opus |
| `tray_app.py` | **系统托盘应用** — 双击启动，自动缩到右下角托盘，右键菜单控制 |
| `windows_protocol.py` | 当前用户的 `localreadtranslate://` 注册、修复与 `start`、`ollama`、`remote` 固定 action 校验 |
| `windows_startup.py` | Windows Startup 快捷方式管理，用于托盘开机自启 |
| `Kokoro TTS.bat` | 推荐的托盘启动器，不依赖 `.pyw` 文件关联 |
| `Kokoro TTS.pyw` | 托盘应用启动器（无黑窗口） |
| `tts-userscript.js` | Tampermonkey 油猴脚本，划词朗读/翻译并显示后台发现的真实来源与模型 |
| `docs/greasyfork-additional-info.md` | Greasy Fork 发布页“附加信息”可直接粘贴的 Markdown |
| `requirements.txt` | Python 依赖清单 |
| `start.bat` | 一键启动服务器（终端模式，调试用） |
| `install-document-addins.bat` | 当前用户 Word/WPS 加载项安装器 |
| `uninstall-document-addins.bat` | 精确移除本项目 Word/WPS 注册 |
| `setup.bat` | 一键环境配置 |
| `tests/fixtures/latex-formula-corpus.md` | 50 公式原生转换与回环语料 |
| `docs/iteration-8-2026-07-23-office-wps-document-assistant.md` | 当前 Word/WPS 朗读、翻译与公式加载项发布记录 |
| `docs/iteration-7-2026-07-23-installable-office-wps-addins.md` | 历史 Word/WPS 可安装公式加载项发布记录 |
| `docs/iteration-6-2026-07-23-office-wps-latex-interchange.md` | 历史 Office/WPS 公式交换核心发布记录 |
| `docs/iteration-5-2026-07-23.md` | 历史后台驱动翻译与设置组件发布记录 |
| `docs/iteration-4-2026-07-18.md` | 历史服务控制与远程翻译发布记录 |

> 💡 双击 **`Kokoro TTS.bat`** 即可启动托盘程序。

---

## 🔌 API 接口说明

### `POST /tts` — 文本转语音

**请求：**
```json
{
  "text": "Hello, how are you today?",
  "voice": "af_bella",
  "speed": 0.8
}
```
`voice` 可选，默认使用 `af_bella`。`speed` 可选，默认 `0.8`。

**响应：** 默认 `audio/wav`；使用请求头 `Accept: audio/ogg` 或 `?format=ogg` 可返回 OGG/Opus。

**示例（PowerShell）：**
```powershell
Invoke-WebRequest -Uri "http://127.0.0.1:5000/tts" `
  -Method POST `
  -ContentType "application/json" `
  -Body '{"text":"Hello world"}' `
  -OutFile "test.wav"
```

### `POST /tts/stream` — 流式文本转语音

返回 `audio/webm; codecs="opus"`，用于油猴脚本的 MediaSource 连续播放。长文本会在全部生成完成前开始播放。

### `GET /health` — 健康检查

返回 API 与 TTS 两层状态。`api_ready` 表示本地中介服务可以接收请求；`tts_model_loaded` 表示 Kokoro 是否已经按需载入。仅使用翻译时，API 可以正常工作而 TTS 仍未载入。

### `GET /translate/health?model=...` — 翻译来源与模型状态

旧兼容字段继续描述当前选中模型；`sources[]` 分别报告本机和每个托盘已配置远端的安全 ID/名称/类型、可达性以及模型的 `running`、`pinned`、`usable_for_translation`。响应不包含远程 URL、主机、端口或凭据。`available_model_options` 是网页普通选择器唯一使用的扁平列表，已排除 embedding/reranking 模型。

### `POST /translate/model/keepalive` — 常驻当前 Ollama 模型

Advanced 中的 **Load & keep / Keep loaded** 会调用这个接口，并按当前模型来源向本机或项目服务器发送 `keep_alive: -1m`。

### `POST /translate/model/unload` — 卸载当前 Ollama 模型

Advanced 中的 **Unload** 会先移除中介 pin，再检查当前模型所属来源。模型已经不在运行列表时直接返回 `unloaded`，不会为了卸载而发送生成请求、重新加载模型；仍在运行时才发送显式 `keep_alive: 0`。只有运行列表确认模型消失才返回 `unloaded`；超时仍在运行则返回 `still_running`，不会误报显存已释放。

### `GET /document/latex/health` — 原生公式转换状态

返回本机 Pandoc 转换层是否可用、交换格式 `latex` 和原生格式 `docx-omml`，但不会暴露本机可执行文件路径。

### `POST /document/latex-fragment` — LaTeX 段落转原生公式片段

```json
{
  "text": "第一段有 $x^2$。\\n\\n$$\\n\\\\frac{a}{b}\\n$$"
}
```

返回规范 LaTeX、Word 使用的 `docx_base64`、WPS 使用的一小时本机 `local_path`，以及行内、独立和原生公式计数。输入必须至少包含一个可识别公式。

### `POST /document/native-to-latex` — 原生选区转 LaTeX

```json
{
  "source_format": "flat-opc",
  "content": "<pkg:package>...</pkg:package>"
}
```

支持 Word 的 `flat-opc`、兼容保留的 `docx-base64`，以及 WPS 加载项使用的 `docx-local-path`。本地路径只能是当前用户临时目录的直属随机一次性 DOCX；服务校验路径、文件名、大小、ZIP 条目和 DOCX 结构。接口只返回规范纯文本 LaTeX、公式计数和 warning。

### `GET /` — 内置测试页面（浏览器访问 http://127.0.0.1:5000）

---

## ❓ 常见问题

### Q：和 Ollama 有什么关系？
A：Ollama 用于翻译和公式口语化，可以来自本机，也可以来自你在托盘程序中配置的项目服务器；Kokoro TTS 始终在本机负责朗读。两者都由本地 FastAPI 服务调度，浏览器脚本只请求 `127.0.0.1:5000`。

### Q：Greasy Fork 发布页的 GitHub 地址放哪里？
A：脚本元信息里使用 `@homepageURL` 指向 GitHub 项目主页，`@supportURL` 指向 GitHub Issues；发布页“附加信息”正文里也建议再放一次项目地址。可直接使用 `docs/greasyfork-additional-info.md` 的内容。

### Q：推理速度如何？
A：RTX 4070 Super 上，一句普通英文几乎瞬间生成（<0.5 秒）。Kokoro 只有 82M 参数，极其轻量。

### Q：如何更换声音？
A：优先在网页齿轮面板或托盘菜单中选择声音；命令行启动时也可设置 `KOKORO_VOICE` 环境变量，单次 API 请求可指定 `"voice": "am_liam"` 等。

### Q：eSpeak-NG 安装后仍报错？
A：确认已将 `C:\Program Files\eSpeak NG` 添加到系统 Path 环境变量，且**重启了终端**。

### Q：油猴脚本不工作？
A：先确认 Tampermonkey 脚本已启用，再点击设置面板中的 **Start local service**。如果浏览器没有弹出“打开外部应用”提示，或点击后服务仍未启动，可在项目目录运行 `conda run -n kokoro-tts python windows_protocol.py register` 修复协议注册，然后访问 `http://127.0.0.1:5000/health` 验证。

### Q：为什么项目服务器没有模型？
A：先在 Translation 中选择 **Project Server**。未连接时点击 **Connect**，它只负责打开托盘的 `Remote Service` 窗口；在托盘中保存并连接后，来源行会自动变为 **Connected**，只显示该服务器实际存在的生成模型，不会再要求重复连接。如果远端只有 embedding/reranking 模型，模型列表仍会为空。

### Q：为什么没有本地模型？
A：先在 Translation 中选择 **Local Ollama**。离线时点击 **Start**，托盘会启动已安装的 `ollama serve`；在线但列表为空时，请先用 `ollama pull <模型名>` 拉取生成模型。网页不会把默认名称或项目服务器模型伪装成本机模型。

### Q：现在可以直接安装 Word/WPS 插件吗？
A：可以。先保存并关闭 Word/WPS，双击 `install-document-addins.bat`，再重开应用。当前入口为 Word 的 **LocalReadTranslate 文档工作台** 和 WPS 的 **阅读与公式**。Word 与 WPS 的真实公式按钮双向链路均已通过；WPS Writer 12.1.0.26895 已确认两个 LaTeX 公式可转为内部可编辑公式，再准确复制为一份规范 LaTeX 文本。新增翻译/朗读目前已通过加载项代理集成测试，但尚未冒充真实宿主按钮验收。详细入口和排障见 `addons/README.md`。

### Q：移动项目目录后 **Start local service** 失效怎么办？
A：协议注册记录的是环境 `pythonw.exe` 和项目 `tray_app.py` 的绝对路径。移动项目或环境后重新运行 `conda run -n kokoro-tts python windows_protocol.py register` 即可修复；注册在当前用户的 `HKCU` 下，不需要管理员权限。

---

## ⚙️ 高级配置

服务配置优先通过环境变量设置，例如：

```powershell
$env:KOKORO_HOST = "127.0.0.1"
$env:KOKORO_PORT = "5000"
$env:KOKORO_VOICE = "af_bella"
$env:KOKORO_SPEED = "0.8"
$env:KOKORO_DEVICE = "auto"  # auto / cuda / cpu
```

油猴脚本固定访问本机回环服务。远程主机、凭据和 Ollama 地址只在托盘程序中配置，不应写进 `tts-userscript.js`。

## 远程 Ollama（局域网服务器）

右键点击 Kokoro TTS 托盘图标，选择 `Remote Service`。程序预填了 `10.12.96.203`，但默认不开启远端连接，普通启动仍优先使用本地。连接方式可选：

- `ssh`：优先使用 SSH agent、默认密钥或 `~/.ssh/config` 中匹配的密钥；也可明确填写 key 文件。只有密钥认证失败且填写了密码时，才回退到密码认证。客户端加载系统/OpenSSH 主机密钥并拒绝未知主机，验证通过后才为远端 Ollama 建立本地隧道。
- `api`：直接填写原生 Ollama API 地址，例如 `http://10.12.96.203:11434`，不建立 SSH 隧道。当前实现不会添加 API key 或其他认证请求头，因此只应在可信局域网或 VPN 中使用，不要把未加密的 HTTP Ollama 端口暴露到公网。

Translation 中先选择 **Project Server**：未连接时显示 **Connect** 并打开托盘窗口；连接成功后来源行变为 **Connected**，只出现 `服务器名称 / 模型名`，不再要求重复连接。每个来源分别记住最后一个有效模型；模型失效时只在同一来源内选择替代项，不会静默切换来源。Ollama 请求会绕过系统 HTTP 代理，避免局域网地址被代理拦截，也避免选中文本经过无关代理。

浏览器脚本只连接本机 `127.0.0.1:5000`，不会拿到 SSH 密码或密钥路径。为了使用方便，托盘程序会把远程配置保存在已被 Git 忽略的 `tray_settings.json`。如果填写了 SSH 密码，它会以明文保存在该文件中；请保护 Windows 账户和项目目录，优先使用 SSH agent 或密钥文件，不要同步、提交或分享该文件。

SSH 主机身份采用失败即关闭策略：客户端调用 `load_system_host_keys()` 并使用 Paramiko `RejectPolicy`。只有通过可信渠道核对指纹后，才应把主机加入 `known_hosts`。本机已经存在 `10.12.96.203` 的记录，并已用此策略实机重连成功。

Kokoro TTS 模型只在第一次点击 Read 时按需加载。仅启动 API 或使用远端翻译不会初始化 Torch/Kokoro，也不会占用本地 GPU；`/health` 会分别报告 `api_ready` 与 `tts_model_loaded`。

## Tampermonkey 更新与发布

仓库中的 `tts-userscript.js` 当前版本为 `1.15.3`，FastAPI 版本为 `1.7.18`。修改仓库文件不会自动替换浏览器里已经安装的副本，发布时应按以下顺序操作：

1. 在 Tampermonkey 编辑器中加载或粘贴最新脚本进行本地验证。
2. 每次发布都递增脚本头部的 `@version`，并运行下方完整验证命令。
3. 推送到 GitHub 后，确认脚本的 Raw 地址已经返回新版本。
4. 在 Tampermonkey 中执行“检查用户脚本更新”，确认已安装副本升级。
5. Greasy Fork 发布页使用同一个版本号，并同步更新附加信息。

## 发布前验证

```powershell
conda run -n kokoro-tts python -m pytest tests -v
conda run -n kokoro-tts python -m py_compile server.py document_formula.py addon_host.py addin_registration.py audio_encoding.py tray_app.py "Kokoro TTS.pyw" tts_catalog.py windows_protocol.py windows_runtime.py windows_startup.py scripts/sync_catalog.py
node --check tts-userscript.js
node --test tests/office-addins-core.test.cjs tests/userscript-core.test.cjs
conda run -n kokoro-tts python scripts/sync_catalog.py --check
conda run -n kokoro-tts python -c "from audio_encoding import validate_ffmpeg; validate_ffmpeg()"
conda run -n kokoro-tts python -m pip check
git diff --check
```

本次文档助手加载项发布记录见 [`docs/iteration-8-2026-07-23-office-wps-document-assistant.md`](docs/iteration-8-2026-07-23-office-wps-document-assistant.md)；iteration 7 保存可安装公式壳层与宿主实机证据，iteration 6 保存公式交换核心历史，iteration 5 继续保存后台驱动来源/模型设置记录，iteration 4 保留为更早历史。
